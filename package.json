
{
  "name": "nomo",
  "private": true,
  "description": "Self-hostable AI trading chat for Robinhood agentic accounts",
  "license": "MIT",
  "workspaces": [
    "shared",
    "server",
    "frontend"
  ],
  "scripts": {
    "prepare": "node scripts/install-hooks.mjs",
    "dev": "concurrently -n server,frontend -c blue,green \"npm run dev --workspace server\" \"npm run dev --workspace frontend\"",
    "build": "npm run build --workspace shared && npm run build --workspace server && npm run build --workspace frontend",
    "typecheck": "npm run typecheck --workspaces"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=20"
  }
}
